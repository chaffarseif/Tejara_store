import 'package:flutter/material.dart';

class ThemeSelectorPresentation extends StatelessWidget {
  const ThemeSelectorPresentation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Theme Selector')));
  }
}
